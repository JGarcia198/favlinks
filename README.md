# FavLinks

A simple React app to save your favorite links.

## Features
- Add a link (name + URL)
- View links in a table
- Delete links
- Click a URL to open it in a new tab

## Run locally
```bash
npm install
npm start
